apiVersion: apps/v1
kind: Deployment
metadata:
  name: artur-micro
  labels:
    app: artur-micro
  namespace: artur
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artur-micro
  template:
    metadata:
      labels:
        app: artur-micro
    spec:
      containers:
        - name: artur-micro
          image: ramann123/ssecure-raman-customer-orders-service:latest
          ports:
            - containerPort: 5000
          resources:
            limits:
              cpu: "250m"
              memory: "256Mi"

